# C++ Portfolio

Репозиторий с моими проектами на C++ для демонстрации навыков.

## Проекты

- **HTTP Server** — многопоточный HTTP-сервер с пулом потоков и поддержкой статики.
- **In-Memory Cache** — key-value хранилище с TTL, сетевым интерфейсом и snapshot-ами.

## Сборка

```bash
mkdir build && cd build
cmake ..
make
